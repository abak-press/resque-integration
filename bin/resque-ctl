#!/usr/bin/env ruby
# coding: utf-8

# Скрипт, позволяющий запускать/останавливать resque
# Должен запускаться из директории с проектом!

require 'bundler/setup'
require 'pathname'
require 'resque/integration/cli'

root = Pathname.new(Dir.pwd)
cli = Resque::Integration::CLI.new(root)
cli.run